---
import type { BrandData } from "@/types/brand";
import KpForm from "../blocks/KpForm.astro";

interface Props {
  brandData: BrandData;
}

const { brandData } = Astro.props;
---

<section id="kp" class="bg-kp-surface overflow-clip scroll-mt-[100px]" data-cmp="KP">
  <div class="container">
    <div class="grid grid-cols-1 lg:grid-cols-[1fr_2fr] gap-3 lg:gap-10">
      <div class="self-end origin-bottom lg:scale-130 pointer-events-none order-last lg:order-first">
        <img class="" src="/images/kp-image.webp" alt="" loading="lazy" />
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-[1fr_365px] gap-4">
        <h2 class="max-w-[516px] py-5 lg:py-12.5 text-left typo-h3 text-kp-text *:text-kp-text-accent *:not-italic">
          Бесплатно <em>сделаем</em> коммерческое предложение <em>индивидуально под ваши потребности</em>
        </h2>

        <KpForm />
      </div>

      
    </div>
  </div>
</section>
